{
    "version": 2,
    "env": {
        "CLIENT_URL": "https://medical-test-tracking.vercel.app"
    },
    "builds": [
        {
            "src": "backend/main.ts",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "backend/main.ts",
            "methods": ["GET", "POST", "PUT", "PATCH", "DELETE", "OPTIONS"]
        }
        
    ]
}